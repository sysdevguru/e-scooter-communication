# Omni E-Scooter IoT communication module via TCP socket.

This fragment of codebase is purposed for showing how to communicate omni e-scooter IoT via TCP socket following IoT communication protocol.
